repos:
  - repo: local
    hooks:
      - id: poe-format
        name: Formatting
        entry: poe format
        language: system
        types: [python]

      - id: poe-lint
        name: Linting
        entry: poe lint
        language: system
        types: [python]

      # - id: pytest
      #   name: Testing
      #   entry: pytest
      #   language: system
      #   types: [python]
